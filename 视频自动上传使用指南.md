# 视频自动上传使用指南

## 快速开始

### 1️⃣ 首次配置（仅需一次）

#### 登录抖音账号获取 Cookie

打开终端，执行以下命令：

```powershell
cd f:\公众号写作\social-auto-upload
.\venv\Scripts\activate
python cli_main.py douyin my_account login
```

**操作说明**：
- 执行后会弹出浏览器窗口
- 使用抖音 APP 扫码登录创作者平台
- 登录成功后，Cookie 会自动保存到 `cookies/douyin_my_account.json`
- **重要**：如果想使用其他账号名，请修改 `auto_upload_bridge.py` 中的 `DOUYIN_ACCOUNT` 配置

---

### 2️⃣ 启动自动上传服务

双击运行：
```
f:\公众号写作\start_auto_upload.bat
```

或在终端执行：
```powershell
cd f:\公众号写作
python auto_upload_bridge.py
```

**服务启动后会**：
- 每 60 秒扫描一次 `MoneyPrinterTurbo/storage/tasks/` 目录
- 自动检测新生成的 `final-*.mp4` 视频
- 提取字幕作为标题
- 调用 social-auto-upload 上传到抖音
- 记录上传历史，避免重复上传

---

### 3️⃣ 停止服务

在运行窗口按 `Ctrl + C` 即可停止。

---

## 配置说明

### 修改抖音账号名

编辑 `auto_upload_bridge.py`，修改第 17 行：

```python
DOUYIN_ACCOUNT = "your_account_name"  # 改成你的账号名
```

### 调整扫描间隔

修改第 20 行：

```python
MONITOR_INTERVAL = 120  # 改成 120 秒（2分钟）
```

### 修改重试次数

修改第 23 行：

```python
MAX_RETRIES = 5  # 改成 5 次
```

---

## 日志与历史

### 查看运行日志

```
f:\公众号写作\logs\auto_upload_bridge.log
```

日志包含：
- 视频检测记录
- 上传进度
- 错误信息

### 查看上传历史

```
f:\公众号写作\upload_history.json
```

记录格式：
```json
{
  "uploads": [
    {
      "file_hash": "abc123...",
      "file_path": "f:\\公众号写作\\MoneyPrinterTurbo\\storage\\tasks\\xxx\\final-1.mp4",
      "task_id": "3266d12a-db3a-4904-acd7-58c25ee788c3",
      "upload_time": "2026-01-08T12:30:00",
      "status": "success",
      "platform": "douyin",
      "account": "my_account"
    }
  ]
}
```

---

## 常见问题

### ❓ 提示"未找到抖音 Cookie"

**原因**：未执行登录步骤  
**解决**：按照"首次配置"章节执行登录命令

---

### ❓ 上传失败，提示"Connection error"

**原因**：代理环境变量干扰  
**解决**：
1. 检查 `start_auto_upload.bat` 是否清除了代理
2. 手动在终端执行：
   ```powershell
   $env:HTTP_PROXY=''
   $env:HTTPS_PROXY=''
   python auto_upload_bridge.py
   ```

---

### ❓ Cookie 过期怎么办？

**现象**：日志显示"登录失效"或"401 Unauthorized"  
**解决**：重新执行登录命令：
```powershell
cd social-auto-upload
python cli_main.py douyin my_account login
```

---

### ❓ 如何手动上传单个视频？

不启动监控服务，直接执行：
```powershell
cd social-auto-upload
.\venv\Scripts\activate
python cli_main.py douyin my_account upload "视频路径.mp4"
```

---

## 进阶功能

### 启用 AI 标题优化

编辑 `social-auto-upload/conf.py`：

```python
AI_OPTIMIZER_ENABLED = True
AI_PROVIDER = "zhipu"
AI_API_KEY = "你的智谱API密钥"
AI_BASE_URL = "https://open.bigmodel.cn/api/paas/v4"
```

### 定时发布

修改上传命令，添加定时参数：
```python
# 在 auto_upload_bridge.py 的 upload_to_douyin 函数中
cmd = [
    sys.executable,
    str(SOCIAL_UPLOAD_DIR / "cli_main.py"),
    "douyin",
    DOUYIN_ACCOUNT,
    "upload",
    str(video_path),
    "-pt", "1",  # 定时发布
    "-t", "2026-01-08 18:00"  # 发布时间
]
```

---

## 技术支持

如遇问题，请检查：
1. `logs/auto_upload_bridge.log` - 桥接服务日志
2. `social-auto-upload/logs/` - 上传器日志
3. Cookie 文件是否存在且未过期

**Island Navigator 随时待命** 🏝️
