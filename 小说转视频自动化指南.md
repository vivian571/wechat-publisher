# 小说转视频自动化指南

本指南将教你如何将 `<小说/撰写提示词书籍>` 目录下的文本内容，批量自动转换为短视频，并（可选）自动上传到抖音。

## 1. 准备工作

确保你已经完成了以下部署：
- [x] MoneyPrinterTurbo (视频生成器)
- [x] social-auto-upload (视频上传器)
- [x] auto_upload_bridge.py (自动上传桥接服务)

## 2. 核心脚本：Batch Fiction to Video

我为你创建了一个强大的批量生成脚本：`f:\公众号写作\MoneyPrinterTurbo\batch_fiction_to_video.py`

**功能特点**：
- 📂 **全目录扫描**：自动递归扫描 `小说/撰写提示词书籍` 下的所有 TXT 章节。
- ⏭️ **智能去重**：已经生成过的章节会自动跳过，避免重复消耗 Token。
- 🤖 **AI 文案改写**：调用智谱 AI 将原文改写为更吸引人的短视频文案（开头有钩子、语言口语化、有金句）。
- 📝 **自动截取**：默认读取每章前 1000 字用于改写。
- 🔗 **无缝集成**：直接调用 MoneyPrinterTurbo 内核，无需手动操作 WebUI。

## 3. 操作流程

### 第一步：启动自动上传监控 (可选)

如果你希望生成的视频自动发到抖音，**必须先启动这个**：

双击运行：`f:\公众号写作\start_auto_upload.bat`

> 保持这个黑色窗口打开，它会像雷达一样监控生成的视频。

---

### 第二步：运行批量生成脚本

打开一个新的终端（PowerShell），执行以下命令：

```powershell
cd f:\公众号写作\MoneyPrinterTurbo

# 激活虚拟环境
.\venv\Scripts\activate

# 清除代理干扰（重要！）
$env:HTTP_PROXY=''; $env:HTTPS_PROXY=''; $env:NO_PROXY='open.bigmodel.cn,localhost'

# 开始批量印钞
python batch_fiction_to_video.py
```

### 第三步：坐等收货

1. **观察日志**：你会看到脚本正在逐个处理文件：
   ```
   📖 开始处理章节: 1.1 什么是提示词工程...
   🚀 提交任务 ID: xxx-xxx...
   ✅ 生成完成！
   ```
2. **查看成品**：生成的视频保存在 `MoneyPrinterTurbo/storage/tasks/` 下。
3. **自动上传**：一旦生成完成，`auto_upload_bridge` 窗口会立即检测到，并自动开始上传到抖音。

---

## 4. 高级配置

### 调整文案改写风格

脚本会自动调用 AI 将原文改写为短视频文案。如果你想调整改写风格，可以编辑 `batch_fiction_to_video.py` 中的 `rewrite_for_video` 函数里的 `prompt`：

```python
prompt = f"""你是一位短视频文案专家。请将以下书籍内容改写为一段吸引人的短视频文案（60秒以内）。

要求：
1. 开头必须有强烈的钩子，吸引观众停留
2. 语言要口语化、有节奏感，适合朗读
3. 突出核心观点和金句
4. 结尾要有行动号召或引发思考
5. 控制在 200-300 字以内
...
```

### 其他配置

如果你想调整生成逻辑，可以编辑 `batch_fiction_to_video.py` 顶部的配置区：

```python
# 每个视频最大字数（建议不超过 1200，否则生成太慢）
MAX_CHARS = 1000

# 视频尺寸 (9:16 竖屏 / 16:9 横屏)
# 在 process_chapter 函数里修改 video_aspect="9:16"
```

## 5. 常见问题

Q: **生成速度很慢？**
A: 正常的。每生成一个 3 分钟的视频，因为涉及 TTS 朗读和视频素材下载拼接，大概需要 3-5 分钟。脚本会自动排队处理，你尽管去休息。

Q: **报错 "ImportError"?**
A: 请确保你是在 `MoneyPrinterTurbo` 目录下，并且激活了 `venv` (看到命令行前面有 `(venv)` 标志)。

Q: **生成的视频没有字幕？**
A: 请确认你的电脑是否安装了 ImageMagick。如果没有，视频会有声音但无字幕。

---

**Island Navigator** 🏝️
