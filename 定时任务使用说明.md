# 每日自动化定时任务使用说明

## 📋 概述

已为你配置好每天晚上8点自动执行的公众号矩阵发布任务。

## 📁 相关文件

| 文件 | 说明 |
|------|------|
| `daily_auto_publish.py` | 主控脚本，协调所有发布流程 |
| `run_daily_publish.bat` | 批处理启动脚本 |
| `setup_scheduled_task.ps1` | Windows 任务计划配置脚本 |
| `logs/daily_publish_YYYYMMDD.log` | 每日执行日志 |
| `logs/summary_YYYYMMDD.txt` | 每日摘要报告 |

## 🚀 快速开始

### 1. 配置定时任务

**以管理员身份运行 PowerShell**，然后执行：

```powershell
cd f:\公众号写作
.\setup_scheduled_task.ps1
```

执行后会看到：
```
✅ 任务计划创建成功！
任务详情:
  名称: 公众号矩阵每日自动发布
  执行时间: 每天晚上 20:00
  ...
```

### 2. 验证配置

打开任务计划程序查看：

```powershell
taskschd.msc
```

在任务计划程序库中找到 **"公众号矩阵每日自动发布"**。

### 3. 手动测试

在配置完成后，建议先手动运行一次测试：

```powershell
# 方法1: 通过任务计划手动触发
Get-ScheduledTask -TaskName "公众号矩阵每日自动发布" | Start-ScheduledTask

# 方法2: 直接运行批处理文件
.\run_daily_publish.bat

# 方法3: 直接运行 Python 脚本
python daily_auto_publish.py
```

## 📊 执行流程

```
20:00 开始
  ↓
📱 发布公众号矩阵（4个账号）
  ├─ Account_A_CrossBorder
  ├─ Account_B_English
  ├─ Account_C_Life
  └─ Account_D_Tech
  ↓
🌍 发布国际平台
  ├─ Dev.to
  └─ Hashnode
  ↓
📊 生成执行报告
  ↓
✅ 完成
```

## 📝 日志查看

### 实时日志

```powershell
# 查看今天的日志
Get-Content "logs\daily_publish_$(Get-Date -Format 'yyyyMMdd').log" -Tail 50 -Wait
```

### 摘要报告

```powershell
# 查看今天的摘要
Get-Content "logs\summary_$(Get-Date -Format 'yyyyMMdd').txt"
```

## 🔧 管理任务

### 查看任务状态

```powershell
Get-ScheduledTask -TaskName "公众号矩阵每日自动发布" | Get-ScheduledTaskInfo
```

### 禁用任务

```powershell
Disable-ScheduledTask -TaskName "公众号矩阵每日自动发布"
```

### 启用任务

```powershell
Enable-ScheduledTask -TaskName "公众号矩阵每日自动发布"
```

### 修改执行时间

```powershell
# 例如改为每天早上7点
$Trigger = New-ScheduledTaskTrigger -Daily -At "07:00"
Set-ScheduledTask -TaskName "公众号矩阵每日自动发布" -Trigger $Trigger
```

### 删除任务

```powershell
Unregister-ScheduledTask -TaskName "公众号矩阵每日自动发布" -Confirm:$false
```

## ⚙️ 配置说明

### 任务设置

- **执行时间**：每天 20:00
- **超时时间**：2小时
- **网络要求**：需要网络连接
- **电源设置**：使用电池时也运行
- **错过补运行**：是

### 前置条件

每个账号目录下需要准备好：
1. `output.txt` - 文章内容
2. `cover.png` - 封面图片（可选）

如果缺少这些文件，该账号会被跳过。

## ❗ 注意事项

### 1. 登录状态

- 首次运行需要手动扫码登录
- 登录状态会保存在 `.browser_data` 目录
- 建议定期（每周）手动运行一次，确保登录状态有效

### 2. 内容准备

定时任务**不会自动生成内容**，只负责发布。你需要：
- 提前准备好文章内容（`output.txt`）
- 或者添加内容生成步骤到脚本中

### 3. 错误处理

- 单个账号失败不会影响其他账号
- 所有错误都会记录在日志中
- 建议每天查看摘要报告

### 4. 网络要求

- 需要稳定的网络连接
- 如果使用代理，确保代理配置正确

## 🔄 自动内容生成（可选）

如果想要完全自动化（包括内容生成），可以修改 `daily_auto_publish.py`：

```python
def generate_content(self):
    """生成今日内容"""
    # 调用 AI 生成内容
    # 保存到各账号的 output.txt
    pass

def run(self):
    # 在发布前先生成内容
    self.generate_content()
    
    # 然后发布
    self.publish_wechat_accounts()
    self.publish_international_platforms()
```

## 📞 故障排查

### 问题1：任务没有执行

**检查**：
```powershell
# 查看任务状态
Get-ScheduledTask -TaskName "公众号矩阵每日自动发布"

# 查看上次运行结果
Get-ScheduledTask -TaskName "公众号矩阵每日自动发布" | Get-ScheduledTaskInfo
```

**可能原因**：
- 任务被禁用
- 计算机在执行时间关机
- 任务计划服务未运行

### 问题2：执行失败

**检查日志**：
```powershell
Get-Content "logs\daily_publish_$(Get-Date -Format 'yyyyMMdd').log"
```

**常见原因**：
- 登录过期（重新手动登录）
- 网络问题（检查网络连接）
- 文件缺失（检查 output.txt）

### 问题3：部分账号失败

**查看摘要报告**：
```powershell
Get-Content "logs\summary_$(Get-Date -Format 'yyyyMMdd').txt"
```

找到失败的账号，单独手动发布测试。

## 📈 监控建议

1. **每天查看摘要报告**，确认发布状态
2. **每周检查一次日志**，发现潜在问题
3. **每月手动运行一次**，验证流程正常

## 🎯 最佳实践

1. **提前准备内容**：在晚上8点前准备好所有文章
2. **保持登录**：定期手动运行，刷新登录状态
3. **备份配置**：定期备份 `.env` 和配置文件
4. **监控日志**：设置日志清理策略，避免占用过多空间

---

**最后更新**：2026年1月5日
