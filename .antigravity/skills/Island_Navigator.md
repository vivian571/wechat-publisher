# Agent Skill: Island Navigator

## Metadata
- **Trigger**: "发布", "同步", "调用矩阵", "publish", "deploy", "help"
- **Requires**: `config/island_map.json`

## Instructions
1. **Load Map**: Always read `config/island_map.json` first.
2. **Find Path**: Use the `path` in JSON to locate scripts. Do not guess.
3. **Report**: If a file is missing, report to Yi'an gently.
