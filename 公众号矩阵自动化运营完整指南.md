# 公众号矩阵自动化运营完整指南

> 本文档记录了完整的公众号矩阵自动化运营流程，包括内容生成、多平台发布、问题排查等实用命令和技巧。

---

## 📋 目录

1. [环境准备](#环境准备)
2. [公众号矩阵发布](#公众号矩阵发布)
3. [多领域内容生成](#多领域内容生成)
4. [国际平台发布](#国际平台发布)
5. [常见问题排查](#常见问题排查)
6. [实用技巧](#实用技巧)

---

## 环境准备

### 项目目录结构

```
f:\公众号写作\
├── My_Wechat_Matrix\          # 微信公众号矩阵
│   ├── accounts\               # 账号目录
│   │   ├── Account_A_CrossBorder\
│   │   ├── Account_B_English\
│   │   ├── Account_C_Life\
│   │   └── Account_D_Tech\
│   ├── wechat_auto_post.py    # 自动发布脚本
│   └── generator.py            # 内容生成脚本
├── OmniPublish\                # 国际平台发布工具
│   ├── articles\               # 文章目录
│   ├── adapters\               # 平台适配器
│   └── main.py                 # 主程序
└── temp\wechat_publisher\      # 临时发布工具
    ├── documents\              # 文档目录
    └── Prompts\                # 提示词模板
```

### 必需的 Python 包

```bash
pip install DrissionPage httpx loguru python-frontmatter beautifulsoup4 Pillow
```

---

## 公众号矩阵发布

### 1. 单个账号发布

```bash
# 基本用法
cd f:\公众号写作\My_Wechat_Matrix
python wechat_auto_post.py --account Account_A_CrossBorder

# 使用无头模式（后台运行）
python wechat_auto_post.py --account Account_A_CrossBorder --headless
```

### 2. 批量发布多个账号

```bash
# 依次发布 4 个账号
python wechat_auto_post.py --account Account_A_CrossBorder
python wechat_auto_post.py --account Account_B_English
python wechat_auto_post.py --account Account_C_Life
python wechat_auto_post.py --account Account_D_Tech
```

### 3. 清理浏览器缓存（解决登录冲突）

```bash
# 删除特定账号的浏览器数据
Remove-Item -Path ".browser_data\Account_D_Tech" -Recurse -Force

# 重新发布
python wechat_auto_post.py --account Account_D_Tech
```

### 4. 文章内容准备

每个账号目录下需要准备：
- `output.txt` - 文章正文（纯文本格式）
- `cover.png` 或 `cover_fixed.png` - 封面图片（推荐 2.35:1 比例）

**文章格式示例**：

```
文章标题

正文第一段...

一、小标题一

内容...

二、小标题二

内容...
```

---

## 多领域内容生成

### 1. 查看提示词模板

```bash
cd f:\公众号写作\temp\wechat_publisher\Prompts

# 查看可用的提示词
dir
# AI流习社提示词.md
# 开源智核提示词.md
# 平凡日子记提示词.md
```

### 2. 生成内容的关键要素

#### AI流习社（技术评测）
- **风格**：专业、客观、逻辑严密
- **结构**：开篇引入 → 核心理念 → 上手体验 → 实战案例 → 槽点分析
- **图片**：每个段落后插入 Markdown 图片

#### 开源智核（GitHub 项目推荐）
- **风格**：口语化、有梗、接地气
- **小标题**：灵活多变（如"出道即巅峰"、"爽点在哪？"）
- **必需元素**：项目链接、性能数据、使用场景

#### 平凡日子记（生活哲学）
- **风格**：温暖、治愈、富有逻辑
- **金句**：自然融入正文，不用"金句："标注
- **图片**：配合意境的高质量图片

### 3. 图片链接格式

```markdown
# Unsplash（不稳定，已弃用）
![描述](https://source.unsplash.com/800x600/?关键词)

# Pexels（推荐）
![描述](https://images.pexels.com/photos/图片ID/pexels-photo-图片ID.jpeg?auto=compress&cs=tinysrgb&h=650&w=940)
```

---

## 国际平台发布

### 1. 文章格式要求

**Frontmatter 示例**：

```yaml
---
title: "Your Article Title"
published: true              # true=直接发布, false=保存为草稿
description: "Article description"
tags: ai, deepseek, machinelearning, opensource  # 逗号分隔
cover_image: https://images.pexels.com/photos/xxx.jpeg
canonical_url: null
---

## Your Content Here

Content goes here...
```

### 2. 发布到 Dev.to 和 Hashnode

```bash
cd f:\公众号写作\OmniPublish

# 发布单篇文章
python main.py --file articles/your-article.md --skip-validation

# 自动发布所有文章
python main.py --mode auto --skip-validation
```

### 3. 标签格式要求

**Dev.to**：
- 只允许字母、数字、下划线
- 自动移除连字符和空格

**Hashnode**：
- 只允许小写字母、数字、连字符
- 不能连续使用连字符
- 最多 5 个标签

### 4. 清除发布状态（重新发布）

```bash
# 删除状态文件
Remove-Item .omnipublish\state.json -ErrorAction SilentlyContinue

# 重新发布
python main.py --file articles/your-article.md --skip-validation
```

⚠️ **注意**：删除状态文件会导致重复发布，建议先在平台后台删除旧文章。

---

## 常见问题排查

### 1. 微信发布问题

#### 问题：账号登录冲突
**现象**：文章发布到了错误的账号

**解决方案**：
```bash
# 清理浏览器缓存
Remove-Item -Path ".browser_data\Account_D_Tech" -Recurse -Force

# 重新发布，使用正确的微信账号扫码
python wechat_auto_post.py --account Account_D_Tech
```

#### 问题：封面图片比例不对
**解决方案**：
```bash
# 使用 cover_fixer.py 自动裁剪
python cover_fixer.py --image path/to/cover.png
```

推荐比例：**2.35:1**（宽:高）

#### 问题：编辑器无法定位
**检查选择器**：
```python
# 常用选择器
'.mock-iframe-body'          # 正文区域
'#title'                     # 标题输入框
'#js_editor_insertimage'     # 图片按钮
```

### 2. OmniPublish 发布问题

#### 问题：httpx proxy 参数错误
**错误信息**：`post() got an unexpected keyword argument 'proxies'`

**解决方案**：
```python
# 错误写法
response = httpx.post(url, proxies=self.proxies)

# 正确写法（移除 proxies 参数）
response = httpx.post(url)
```

#### 问题：Hashnode 标签格式验证失败
**错误信息**：`pattern: ^[a-z0-9-]{1,250}...`

**解决方案**：
```python
def _sanitize_tag(self, tag: str) -> dict:
    import re
    slug = tag.lower().replace(' ', '-')
    slug = re.sub(r'[^a-z0-9-]', '', slug)
    slug = re.sub(r'-+', '-', slug)
    slug = slug.strip('-')
    if not slug:
        slug = "general"
    return {"slug": slug, "name": tag}
```

#### 问题：标签解析错误
**现象**：逗号分隔的标签被当成一个整体

**解决方案**：
```python
# 在 parser.py 中添加
raw_tags = post.get('tags', [])
if isinstance(raw_tags, str):
    tags = [tag.strip() for tag in raw_tags.split(',') if tag.strip()]
else:
    tags = raw_tags if raw_tags else []
```

### 3. 进程管理

#### 终止所有 Python 进程
```bash
# 强制终止
taskkill /F /IM python.exe

# 或者使用 PowerShell
Get-Process python | Stop-Process -Force
```

#### 查看运行中的 Python 进程
```bash
Get-Process python | Select-Object Id, ProcessName, StartTime
```

---

## 实用技巧

### 1. 批量复制图片

```bash
# 复制生成的图片到账号目录
Copy-Item "C:\path\to\generated_image.png" "accounts\Account_D_Tech\cover.png" -Force
```

### 2. 快速生成封面图

使用 AI 生成图片的提示词模板：

```
# 技术类
"Modern coding workspace, dark theme, multiple monitors, clean desk setup, professional lighting, high resolution, tech aesthetic"

# 美食类
"Yam and chestnut stewed chicken in clay pot, extreme close-up, appetizing glossy texture, vibrant colors, soft indoor lighting, macro photography"

# 生活类
"Sunrise over mountains, peaceful morning scene, warm colors, inspirational atmosphere, high quality photography"
```

### 3. 文章标题优化技巧

**公众号标题**：
- 使用数字：《这5款工具...》
- 制造悬念：《别再盯着大模型了！》
- 时效性：《2026年...》
- 利益点：《效率翻倍，顺便把钱赚了》

**英文标题**：
- 简洁有力：*What Changed*
- 制造冲击：*Could Change Everything*
- 实用导向：*A Practical Guide*

### 4. 内容发布检查清单

**发布前**：
- [ ] 文章标题吸引人
- [ ] 封面图片比例正确（2.35:1）
- [ ] 正文格式清晰（有小标题）
- [ ] 标签设置合理（2-5个）
- [ ] 图片链接有效

**发布后**：
- [ ] 检查排版是否正常
- [ ] 封面图显示正确
- [ ] 链接可点击
- [ ] 在不同设备预览

### 5. 定时发布策略

**最佳发布时间**：
- 公众号：早上 7-9 点，晚上 8-10 点
- Dev.to：美国东部时间上午（北京时间晚上）
- Hashnode：全天候，但避开周末

### 6. 数据备份

```bash
# 备份重要配置
Copy-Item ".env" ".env.backup"
Copy-Item "config.yaml" "config.yaml.backup"

# 备份发布状态
Copy-Item ".omnipublish\state.json" ".omnipublish\state.json.backup"
```

---

## 快速参考命令

### 微信公众号发布
```bash
# 单账号
python wechat_auto_post.py --account Account_A_CrossBorder

# 清理缓存
Remove-Item -Path ".browser_data\Account_Name" -Recurse -Force
```

### 国际平台发布
```bash
# 发布文章
python main.py --file articles/article-name.md --skip-validation

# 清除状态
Remove-Item .omnipublish\state.json
```

### 进程管理
```bash
# 终止 Python
taskkill /F /IM python.exe

# 查看进程
Get-Process python
```

### 文件操作
```bash
# 复制图片
Copy-Item "source.png" "destination.png" -Force

# 查看目录
dir accounts\
```

---

## 附录：平台特性对比

| 特性 | 微信公众号 | Dev.to | Hashnode |
|------|-----------|--------|----------|
| 语言 | 中文 | 英文 | 英文 |
| 格式 | 纯文本 | Markdown | Markdown |
| 标签数量 | 无限制 | 4个 | 5个 |
| 标签格式 | 任意 | 字母数字下划线 | 小写字母数字连字符 |
| 封面比例 | 2.35:1 | 1000x420 | 任意 |
| 发布状态 | 立即发布 | 可选草稿 | 可选草稿 |
| 图片托管 | 自动上传 | 需外链 | 需外链 |

---

## 总结

本文档涵盖了公众号矩阵自动化运营的完整流程，包括：
- ✅ 4个微信公众号的自动发布
- ✅ 3个领域的中文内容生成
- ✅ 2个国际平台的英文发布
- ✅ 所有常见问题的解决方案

**核心原则**：
1. 内容为王：质量 > 数量
2. 自动化优先：能自动化的绝不手动
3. 多平台布局：国内+国际
4. 持续优化：记录问题，改进流程

---

**最后更新**：2026年1月5日
**维护者**：AI助手
**版本**：v1.0
